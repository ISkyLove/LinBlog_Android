package lin.com.lin.base;

import android.content.Context;
import android.support.v4.view.ViewPager;
import android.util.AttributeSet;

/**
 * @author  Lin
 */
public class LBaseViewPager extends ViewPager {
    public LBaseViewPager(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
